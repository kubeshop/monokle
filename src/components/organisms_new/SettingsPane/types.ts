export enum SettingsPanel {
  Validations = 'validation',
  Plugins = 'plugins-manager',
  ActiveProjectSettings = 'active-project-settings',
  DefaultProjectSettings = 'default-project-settings',
  GlobalSettings = 'global-settings',
}

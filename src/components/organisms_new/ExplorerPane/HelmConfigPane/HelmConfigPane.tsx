import {TitleBar} from '@monokle/components';

const HelmConfigPane = () => {
  return (
    <div>
      <TitleBar expandable isOpen title="Helm Configs" />
    </div>
  );
};

export default HelmConfigPane;

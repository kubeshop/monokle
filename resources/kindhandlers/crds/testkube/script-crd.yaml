---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: 2021-12-10T10:43:52.000Z
  generation: 1
  name: scripts.tests.testkube.io
  resourceVersion: "422318"
  uid: 8bd4954b-1522-4412-8d5f-a01d0a21c67a
spec:
  conversion:
    strategy: None
  group: tests.testkube.io
  names:
    kind: Script
    listKind: ScriptList
    plural: scripts
    singular: script
  scope: Namespaced
  versions:
    - name: v1
      schema:
        openAPIV3Schema:
          description: Script is the Schema for the scripts API
          properties:
            apiVersion:
              description: "APIVersion defines the versioned schema of this representation of
                an object. Servers should convert recognized schemas to the
                latest internal value, and may reject unrecognized values. More
                info:
                https://git.k8s.io/community/contributors/devel/sig-architectur\
                e/api-conventions.md#resources"
              type: string
            kind:
              description: "Kind is a string value representing the REST resource this object
                represents. Servers may infer this from the endpoint the client
                submits requests to. Cannot be updated. In CamelCase. More info:
                https://git.k8s.io/community/contributors/devel/sig-architectur\
                e/api-conventions.md#types-kinds"
              type: string
            metadata:
              type: object
            spec:
              description: ScriptSpec defines the desired state of Script
              type: object
              properties:
                content:
                  description: script content as string (content depends from executor)
                  type: string
                input-type:
                  description: "script content type can be:  - direct content - created from file,
                    - git repo directory checkout in case when test is some kind
                    of project or have more than one file,"
                  type: string
                name:
                  description: script execution custom name
                  type: string
                params:
                  description: execution params passed to executor
                  type: object
                  additionalProperties:
                    type: string
                repository:
                  description: repository details if exists
                  type: object
                  required:
                    - branch
                    - type
                    - uri
                  properties:
                    branch:
                      description: branch/tag name for checkout
                      type: string
                    path:
                      description: if needed we can checkout particular path (dir or file) in case of
                        BIG/mono repositories
                      type: string
                    type:
                      description: VCS repository type
                      type: string
                    uri:
                      description: uri of content file or git directory
                      type: string
                type:
                  description: script type
                  type: string
            status:
              description: ScriptStatus defines the observed state of Script
              type: object
              properties:
                executions_count:
                  type: integer
                last_execution:
                  type: string
                  format: date-time
          type: object
      served: true
      storage: true
      subresources:
        status: {}

